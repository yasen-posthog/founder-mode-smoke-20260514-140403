# Scaffold smoke test

Generated by **PostHog founder mode** from a structured `LandingPageBuildSpec`.

## TL;DR

- Verified hedgehog matching for breeders.
- Built on Next.js + Tailwind.

## Run it

```bash
pnpm install
cp .env.example .env.local  # then set NEXT_PUBLIC_POSTHOG_KEY
pnpm dev
```

Then open http://localhost:3000.

## Sections

- `Hero` — Hero
- `Features` — Features
- `Faq` — FAQ

## Acceptance criteria

- Lighthouse Performance ≥ 85 on mobile
- All images have alt text

## Next steps

- Swap in [shadcn/ui](https://ui.shadcn.com) primitives — `npx shadcn@latest init` then `npx shadcn add button card accordion`.
- Replace the placeholder markdown rendering inside each section with bespoke JSX once the copy is final.
- Wire the explicit PostHog events listed in each section's JSDoc comment.
